import { {{pascalCase name}} } from "/src/features/{{pascalCase featureName}}/{{pascalCase name}}";

let {{pascalCase featureName}} = {
    {{pascalCase name}}: {{pascalCase name}},
}

global.{{pascalCase featureName}} = {{pascalCase featureName}};